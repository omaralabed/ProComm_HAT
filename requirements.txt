# ProComm SIP Phone System - Web UI + I2S HAT Audio
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.9.0
python-engineio>=4.8.0

# Audio (I2S HAT via direct ALSA)
pyalsaaudio>=0.11.0

# High-quality audio resampling (anti-aliased 48kHz ↔ 8kHz/16kHz)
soxr>=0.3.0
numpy>=1.24.0

# G.722 codec for wideband audio (eliminates G.711 quantization hiss)
g722>=1.2.0

# QR code generation for web UI
qrcode[pil]>=8.0
